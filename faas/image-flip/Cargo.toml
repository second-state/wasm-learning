[package]
name = "flip"
version = "0.1.0"
authors = ["ubuntu"]
edition = "2018"

[lib]
name = "flip_lib"
path = "src/lib.rs"
crate-type =["cdylib"]

[dependencies]
image = { version = "0.23.0", default-features = true, features = ["jpeg", "png", "gif"] }
wasm-bindgen = "=0.2.61"
